import CreateBlog from "../../../components/CreateBlog"; 

function CreateBlogPage() {
    return (
        <div className="container">
            <CreateBlog /> {/* Using the CreateBlog component */}
        </div>
    );
}

export default CreateBlogPage;
